letterlist = [1110101, 10010110]

redval = 159

mod = redval % 4
if letterlist[0] == "00":
    if mod == 0:
        pass
    if mod == 1:
        # math to make mod 0
        pass
    if mod == 2:
        # math to make mod 0
        pass
    if mod == 3:
        # math to make mod 0
        pass


if letterlist[0] == "00":
    desiredr = 0
    if desiredr != mod:
        redval = redval + (desiredr - mod)
